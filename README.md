# HarmonyBridge
A plugin module of LenovoLegionToolkit that provides runtime method patching capabilities to other plugins via the Harmony library.

HarmonyBridge wraps Harmony — a .NET runtime patching library — allowing LLT plugins to dynamically intercept and replace method behavior in target assemblies at runtime, without modifying the original source code.
